import interfaces.*;
import client.ClientMessageGenerator;
import controllers.IController;
import controllers.MemoryController;
import logic.Game;
import server.ServerMessageGenerator;
import socketcommunication.GameClient;

public class MainFactory {
/*    private static IClientMessageGenerator clientgenerator;
    private static IGameClient gameClient;

    private static IGame game;
    private static IServerMessageGenerator serverMessageGenerator;
    private static IController controller;

    public static IController getGameHandler(){
        clientgenerator = new ClientMessageGenerator();
        gameClient = new GameClient(clientgenerator);

        controller = new MemoryController();
        game = new Game();
        serverMessageGenerator = new ServerMessageGenerator();
    }*/
}
